using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// TAProject class inherited BaseProject
/// because the parent class also inherited BaseProject
/// and BaseProject inherited MonoBehaviour
/// so the child class will also inherite MonoBehaviour
/// and all the Unity specific classes would work
/// </summary>
public class TAProject : DAProject
{
    // Start is called before the first frame update
    
}
